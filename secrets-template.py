# Specify your local wifi creds here, then rename the file to secrets.py
# .gitignore is configured so secrets.py doesn't get checked in accidentally
ssid_secrets = {
    # NOTE - key must be SSID currently
    'SSID1-NAME' : {
        'ssid' : 'SSID1-NAME',
        'password' : 'SSID1-PASSWORD'
        },
    'SSID2-NAME' : {
        'ssid' : 'SSID2-NAME',
        'password' : 'SSID2-PASSWORD'
        },
    }
